server:
  servlet:
    context-path: /api/v1
  port: 9090
spring:
  application:
    name: azure-storage-acct-cloud-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
azure:
  storage:
    connection-string: ${STORAGE_CONNECTION_STRING}
    container-name: ${CONTAINER_NAME}