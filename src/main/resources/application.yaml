server:
  servlet:
    context-path: /api/v1
  port: 9090
spring:
  application:
    name: product-cloud-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always